steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/profile-site:${SHORT_SHA}'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/profile-site:${SHORT_SHA}'
